/** Design Defaults recovered from MagicPath agent skill */
export const DesignDefaults = [
  'No device mockups — canvas is the frame',
  'Responsive / fluid sizing — no pixel-locked outer shells',
  'Centered in the frame',
  'Single screen per component (multi-view via internal React state)',
  'Fully interactive — real state, not static screenshots',
  'React + Tailwind v4 only in forge template',
  'Editable only: src/App.tsx, src/index.css, src/components/generated/**, assets/**',
] as const;
