# 00-shared

Local-solo foundation for MyPath.

- `routes.ts` — non-SaaS API routes only; default origin `http://127.0.0.1:8787`
- `types.ts` — domain types
- `config.json` — local channel config (no Firebase/Intercom/GA/billing)

SaaS surfaces intentionally removed.
