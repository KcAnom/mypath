{
  "name": "mypath-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "electron": "^36.0.0",
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
